import { OpenDefinition } from "./files/OpenDefinition";


function errorExample(): string{
    return "No errors if this row available"
}

const example: OpenDefinition = new OpenDefinition()
example.hello()
